<!DOCTYPE html>
<html>
  <head>
    <title>Library Management</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="app/assets/stylesheets/custom.css.scss">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

        <header>

          <nav>
            <div class="container">
              <div class="nav-wrapper">
              <a href="#" class="brand-logo right">Library Management</a>
              
                <ul id="nav-mobile" class="left hide-on-med-and-down">
                 <li><%= link_to "Home", root_path %></li>
                
                <% if current_user%>
                    <% if current_user.role.eql?("user") %> 
                      <li><%= link_to "User", current_user %></li>
                      <li><%= link_to "books",books_path%></li>
                      <li><%= link_to "logout",logout_path,data: {turbo_method: :delete} %></li>

                      <% else %>
                        <li><%= link_to "User", users_path %></li>
                        <li><%= link_to "books",books_path%></li>
                        <li><%= link_to "logout",logout_path,data: {turbo_method: :delete} %></li>        
                    <% end %>
                 <% end %>
              
                
              </div>
            </div>
          </nav>
        
        </header>

        <main class="container">
             <%= yield %>
        </main> 


          <footer class="page-footer">
              <div class="container">
                <div class="row">
                  <div class="col l6 s12">
                    <% if current_user %>
                    <h5 class="white-text">Logged in as <strong style="font-weight:bold;"><%= current_user.name.capitalize() %></strong></h5>
                    <p class="grey-text text-lighten-4">Stay active, Stay updated</p>
                    <% end %>
                  </div>
                </div>
              </div>

              <div class="footer-copyright">
                <div class="container">
                © 2022 Copyright Text
                <a class="grey-text text-lighten-4 right" href="#!">Back to top</a>
                </div>
              </div>

        </footer>

  </body>
</html>
